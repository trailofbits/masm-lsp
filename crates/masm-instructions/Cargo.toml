[package]
name = "masm-instructions"
version = "0.1.0"
edition = "2021"

[dependencies]
miden-assembly-syntax = { workspace = true }

[build-dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "1.0"
